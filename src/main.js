const weather = "weather always changing";
console.log(weather);

const lower = weather.toLowerCase();
console.log(lower);

const upper = weather.toUpperCase();
console.log(upper);

const someDays = weather.replace("weather elways changing");
console.log(someDays);

const otherDays = weather.replace("weather always changes");
console.log(otherDays);

const stormOne = "Thunder";
console.log(stormOne);

const stormTwo = "Lighting";
console.log(stormTwo);

const stormThree = stormOne + stormTwo;
console.log(stormThree);

const x = 5;
const y = 5;
const sum = `The sum is ${x + y}.`;
console.log(sum);